version: '3.8'

services:
  web:
    image: app-name:latest
    container_name: web-modules
    build:
      target: web
      context: .
    env_file:
      - .env
    ports:
      - "7000:80"
    volumes:
      - .:/var/www/html
